function [L] = cholesky_dist(A)
    n = length(A);
    L = zeros(n, n);
    for k=1:n
        for i=1:n
            if i == k
                L(k, i) = sqrt(A(k, k) - sum(A(k, 1:k-1)^2));
            else
                L(i, k) = 1/L(k, k) * (A(i, k) - sum(
        end
    end
end